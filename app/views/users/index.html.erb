<h1>COLLECTION</h1>
<h3>Your fashion events, images and videos at your finger tips!</h3>

<%= form_tag sessions_path,{:class => 'form-horizontal'} do %>
    <div class="row"><legend class="col-md-4 col-md-offset-4">A member? Log In:</legend></div>
    <fieldset>
        <div class="form-group">
            <%= label_tag :email, 'Email', class: 'col-md-4 control-label' %>
            <div class="col-md-5">
                <%= text_field_tag :email, params[:email], class: 'form-control' %>
            </div>
        </div>
        <div class="form-group">
            <%= label_tag :password, 'Password', class: 'col-md-4 control-label' %>
            <div class="col-md-5">
                <%= password_field_tag :password, nil, class: 'form-control' %>
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-8 col-md-offset-4">
                <%= submit_tag 'Sign Up', class: 'btn btn-success' %>
                <%= link_to "New to Collection?", new_user_path, class: 'btn btn-primary' %>
            </div>
        </div>
    </fieldset>
<% end %>